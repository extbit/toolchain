project

files (
    (clang clang++ \
     llvm-ar llvm-link llvm-nm \
     llvm-objcopy llvm-objdump \
     llvm-strip lldb) ⇒ $(CWD)/bin
)

# Tools for building C/C++ programs/libraries.
AR := $(file llvm-ar)
CC := $(file clang)
CXX := $(file clang++)
LD := $(file llvm-ld)
OBJCOPY := $(file llvm-objcopy)
OBJDUMP := $(file llvm-objdump)
STRIP := $(file llvm-strip)

# Command flags for C/C++ compilation
ARFLAGS :=
CFLAGS :=
CXXFLAGS :=
LDFLAGS :=
LDLIBS :=
LOADLIBS :=

